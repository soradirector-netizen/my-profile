# Week11 AIと考えたログ

## Part 1：AIと考えた1セッション

### テーマ

Week10で作ったJavaScriptのコードについて、AIにレビューしてもらう。

### 送った指示（良いプロンプトの型）

目的:
ユーザー一覧を表示するJavaScriptコードをレビューして、改善点を知りたい。

前提:
HTMLとJavaScriptだけで作っている。初心者なので、難しい書き方への変更は避けたい。

見てほしい観点:
コードの分かりやすさ、エラーが起きそうな部分、初心者が理解しておいた方がいい部分。

制約:
コード全体を大きく書き換えず、今のコードを活かした改善案にしてほしい。

出力形式:
「問題点・理由・改善案」の順番で説明してほしい。

### 返ってきた出力（要約）

AIから、`response.ok`の使い方や`usernameInput.value.trim()`、`user.name || username`、`console.log(user)`、エラーメッセージの分け方、`createElement`や`appendChild`の使い方などについてレビューしてもらった。

### "なぜ"を聞いた記録

* 聞いたこと:

  * `response.ok`で具体的にどうやって成功・失敗を確認しているのか。
  * なぜ`console.log()`は開発中だけでよいのか。

* 返ってきた説明（要約）:

  * `response.ok`は、GitHub APIから返ってきたレスポンスのHTTPステータスが成功かどうかを確認している。成功なら`true`、失敗なら`false`になる。また、`fetch()`は404などのHTTPエラーでは自動的に`catch`に入らないため、`response.ok`で確認する必要がある。
  * `console.log()`は、APIから取得したデータの中身を開発者が確認するために使う。サイトの機能そのものには必要ないため、デバッグが終わったら削除してもよい。

* 聞く前は分からなかったが、聞いて分かったこと:

  * `fetch()`で通信に成功しても、404などのHTTPエラーでは`catch`に入らないことが分かった。そのため、`response.ok`を使ってレスポンスが成功したか確認する必要がある。また、`console.log()`はユーザーのためではなく、開発者がデータを確認するためのものだと分かった。

### 出力の4分類

| 分類    | 内容                              | 判断理由                                                              |
| ----- | ------------------------------- | ----------------------------------------------------------------- |
| 採用する  | `response.ok`を使ってAPIの成功・失敗を確認する | `fetch()`は404などのHTTPエラーでは`catch`に入らないため、`response.ok`で確認する必要があるから |
| 保留する  | エラーの種類ごとにメッセージを分ける              | 404や403などを細かく判定する方法は便利だが、今回のコードではそこまで必要ないため                       |
| 採用しない | `console.log(user)`を完成後も残しておく   | `console.log()`は開発者がデータを確認するためのもので、サイトの機能には必要ないため                 |
| 要確認   | `innerHTML`を使ってカードを作る方法に変更する    | 今の`createElement`や`appendChild`より短く書けそうだが、安全性や違いをまだ十分に理解していないため   |

---

## Part 2：流された場面の振り返り

Week6でインプットやボタン、JavaScriptが出てきたとき、分からないことが多く、AIにコードを手伝ってもらった。説明はしてもらったが、「なぜ？」と深掘りせず、理解したつもりのまま進めてしまった。

---

## Part 3：今後のAI利用方針

### Week11で気づいたこと

今までのPOSSE課題で、AIを十分に活用できていなかったように感じた。AIを使いながらも、分かったことを紙にメモするなど、自分の頭により残るような工夫をしたい。

### 自分のAI利用ルール

① 質問するときは、目的や知りたいことを具体的に伝える。
② 理解できるまで「なぜ？」を繰り返し聞く。分かりにくい場合は、「中学生でも分かるように」など、伝え方を工夫する。
③ AIから答えをもらっても、そのままコピペせず、自分でコードを入力して動かしてみる。
